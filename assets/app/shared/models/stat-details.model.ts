export class StatDetails {
    duration: number;
    timestamp: number;

    constructor(duration: number, timestamp: number) {
        this.duration = duration;
        this.timestamp = timestamp;
    }
}